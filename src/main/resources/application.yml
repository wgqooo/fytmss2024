spring:
    datasource:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/fytmss2024?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&tinyInt1isBit=false&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
        username: root
        password: 1001
        # druid?????
        druid:
            # ?????????????
            initial-size: 5
            # ??????????
            min-idle: 5
            # ?????????
            max-active: 20
            # ????????????????
            max-wait: 60000
            # ??????????????????timeBetweenEvictionRunsMillis???validationQuery?????????
            test-while-idle: true
            # ?????????????testWhileIdel?????
            time-between-eviction-runs-millis: 60000
            # ????????????????????????????????????(??????????????)
            min-evictable-idle-time-millis: 30000
            # ??????????????sql ?????????(oracle?? select 1 from dual)
            validation-query: select 'x'
            # ????????validationQuery????????,???????,???true
            test-on-borrow: false
            # ????????validationQuery????????,???????,???true
            test-on-return: false
    rabbitmq:
        host: 192.168.88.100
        port: 5672
        username: wgq
        password: 1001
        virtual-host: hadoop
    data:
        redis:
            host: 192.168.88.100
            port: 6379
            database: 0
            password: 1001
            lettuce:
              pool:
                  max-active: 8
                  max-wait: -1ms
                  max-idle: 8
                  min-idle: 0



mybatis:
    mapper-locations: classpath:mappers/**/*Mapper.xml
    type-aliases-package: com.fytmss.beans


# 分页框架
pagehelper:
    reasonable: true
    support-methods-arguments: true
    params: countSql


server:
    port: 9091
    servlet:
        context-path: /api
